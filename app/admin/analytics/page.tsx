'use client';import Link from 'next/link';import {useEffect,useState} from 'react';type D=Record<string,number>;const labels:Record<string,string>={members:'Active members',events:'Active events',eventRegistrations:'Event registrations',opportunities:'Open opportunities',applications:'Applications',listings:'Active listings',completedTransactions:'Completed transactions',campaigns:'Active campaigns',foreverPointsOutstanding:'ForeverPoints outstanding',benefitClaims:'Benefit claims',openModerationReports:'Open moderation reports',openWelfareCases:'Open welfare cases'};export default function Analytics(){const [d,setD]=useState<D|null>(null);const [err,setErr]=useState('');useEffect(()=>{void(async()=>{const r=await fetch('/api/admin/analytics',{cache:'no-store'});const j=await r.json();if(!r.ok){setErr(j.error||'Unable to load analytics');return;}setD(j)})()},[]);return <main className="min-h-screen bg-[#06150d] text-white"><div className="mx-auto max-w-6xl px-4 py-8 sm:px-6"><Link href="/admin" className="text-sm text-white/55">← Admin</Link><h1 className="mt-5 text-4xl font-semibold">Platform Analytics</h1><p className="mt-2 text-white/55">Aggregate operational indicators only. This dashboard does not expose private message content or sensitive welfare details.</p>{err&&<p className="mt-5 rounded-xl border border-red-400/20 bg-red-400/10 p-3 text-sm">{err}</p>}<div className="mt-7 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{d&&Object.entries(labels).map(([k,l])=><section key={k} className="rounded-2xl border border-white/10 bg-white/[0.035] p-5"><div className="text-sm text-white/45">{l}</div><div className="mt-3 text-4xl font-semibold">{Number(d[k]||0).toLocaleString()}</div></section>)}</div></div></main>}
