'use client';
import { useState } from 'react';
export default function ForeverPointsCaseForm(){const [type,setType]=useState('missing_points');const [subject,setSubject]=useState('');const [description,setDescription]=useState('');const [message,setMessage]=useState('');const [busy,setBusy]=useState(false);async function submit(){setBusy(true);setMessage('');const r=await fetch('/api/foreverpoints/cases',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({caseType:type,subject,description})});const b=await r.json();setMessage(r.ok?'Case submitted.':b.error||'Unable to submit case.');if(r.ok){setSubject('');setDescription('');location.reload();}setBusy(false);}return <section className="rounded-3xl border border-white/10 bg-white/[0.04] p-5 sm:p-6"><h2 className="text-xl font-semibold">Report a ForeverPoints problem</h2><p className="mt-2 text-sm text-white/45">History is never edited. Approved refunds and corrections create new compensating transactions.</p><div className="mt-5 space-y-3"><select value={type} onChange={e=>setType(e.target.value)} className="w-full rounded-xl border border-white/10 bg-[#071a11] px-4 py-3"><option value="missing_points">I did not receive my points</option><option value="activity_disputed">I did not do this activity</option><option value="wrong_amount">Wrong number of points</option><option value="incorrect_deduction">Points were deducted incorrectly</option><option value="refund_request">I need a refund</option><option value="duplicate_transaction">This happened twice</option><option value="failed_redemption">Redemption failed</option><option value="system_error">System error</option><option value="other">Other</option></select><input value={subject} onChange={e=>setSubject(e.target.value)} placeholder="Short subject" className="w-full rounded-xl border border-white/10 bg-black/20 px-4 py-3"/><textarea value={description} onChange={e=>setDescription(e.target.value)} placeholder="What happened?" rows={5} className="w-full rounded-xl border border-white/10 bg-black/20 px-4 py-3"/>{message&&<div className="rounded-xl border border-white/10 bg-black/20 p-3 text-sm text-white/65">{message}</div>}<button onClick={()=>void submit()} disabled={busy||!subject.trim()||!description.trim()} className="rounded-xl bg-amber-300 px-5 py-3 font-semibold text-[#07120b] disabled:opacity-40">{busy?'Submitting…':'Submit Case'}</button></div></section>}
